| 题号 | 答案 | 解析 |
|---|---|---|
|1|C|一句话解析：A 误把 REMOVE 当成按值删除（列表里没有 4，于是什么都没删），B 在插入后的列表里把数值 9 当成要删除的元素（混淆下标与元素值），D 误以为 REMOVE 删除的是最后一个元素。|
|2|A|一句话解析：B 把条件写反（偶数减、奇数加）得 -74，C 忽略了 ELSE 分支只累加偶数得 82，D 完全忽略条件把 4 个元素全部相加得 90。|
|3|D|一句话解析：A 只看到数据个数没变就认为两个统计量都不变，B 误以为中位数比平均值更容易被极端值拉动，C 误以为中位数会变化（48 改成 4 后中位数仍是 3）。|
|4|B|一句话解析：A 以为样本量能消除偏差，C 把方便样本误认成随机抽样并进一步推断因果，D 把传输加密误当成数据客观可靠。|
|5|C|一句话解析：A 忽略了 IF 条件（全班平均约为 70.8），B 忘掉最后一次除法只看累加结果 251，D 把分子分母看成及格总分与全班总分（比值约 70.9%）。|
|6|A|一句话解析：B 把 4 位当成 4 字节算出 20000 字节，C 把位深直接当成取值个数（16 种误写成 4 种），D 忽略位深按每像素 1 字节计算并错用 8 位的色彩数。|
|7|D|一句话解析：A 是「带宽大所以延迟低」的典型误解，B 把带宽与可靠性混为一谈（带宽不消除丢包），C 忽略了路由优化、边缘节点等确实能降低延迟的手段。|
|8|B|一句话解析：A 把冗余路径误当成永不丢包，C 误以为包必须走同一路径且严格有序到达，D 误以为丢一个包就要重发全部数据。|
|9|C|一句话解析：A 以为可以直接用域名通信，B 把 TCP/IP 与 HTTP 的职责对调还混入了渲染，D 误以为一个域名只能对应一个 IP（忽略了负载均衡与多台服务器）。|
|10|A|一句话解析：B 忽略字节与位的换算直接用 120÷10，C 换算后忘记除以带宽只留下 960，D 把除法写成乘法得到 960×10。|
|11|D|一句话解析：A 把冗余误当成加密，B 误以为副本不占额外存储与成本（副本反而增加总开销），C 错在以为冗余能保证数据内容正确。|
|12|B|一句话解析：A 把 PII 等同于姓名，C 以为去掉姓名就等于去标识化，D 把数据格式（数字）当成敏感程度的判断依据。|
|13|C|一句话解析：A 把不可逆的哈希当成可逆加密，B 把传输加密的保护范围扩大到服务器数据库，D 把加盐误解成让哈希保持固定（加盐恰恰使每次结果不同）。|
|14|A|一句话解析：B 把认证手段误当成授权，C 把认证误当成加密（加密不判断身份），D 夸大双因素认证的作用并据此放弃强密码。|
|15|D|一句话解析：A 误以为去掉敏感字段就消除了偏差，B 把偏差当成样本量不足，C 忽略了算法结论对真实人群的影响。|
|16|B|一句话解析：A 把隐私风险误当成带宽问题，C 以为签了合同学生就不再是利益相关者，D 误判出勤数据不属于个人信息而免去告知义务。|
|17|D|一句话解析：A 用长度是否为 2 的幂代替了有序性这个前提，B 误以为比较次数与长度无关且能一步命中，C 把确定性的二分查找误当成启发式方法。|
|18|A|一句话解析：B 把随机性当成免检理由，C 以为多次运行能自动纠正假设错误，D 把模拟输出直接当成真实世界的因果结论。|
|19|C|一句话解析：A 把「通用算法不可判定」扩大成「每个具体程序都判断不了」，B 把不可判定误当成工程难题，D 把启发式方法误当成能保证正确判断的算法。|
|20|B|一句话解析：A 误以为启发式方法只要多运行就能收敛到最优，C 把计算代价高误当成不可解，D 把最近邻这种贪心策略误当成精确算法。|
|21|D|一句话解析：A 把实现细节当成目的，B 只有泛泛的夸奖且缺少输入说明，C 把「不报错」当目的并写错了输入来源。|
|22|A|一句话解析：B 把抽象误解为可以省略接口说明，C 把报告写成逐行语法讲解，D 混淆了过程抽象与数据抽象。|
|23|B|一句话解析：A 把通过三个用例等同于程序正确，C 误判为空列表崩溃属于语法错误并想隐藏用例，D 以为重写代码后测试记录可以省略。|
|24|C|一句话解析：A 把公开可见误当成公有领域，B 只做署名却忽略授权与个人信息，D 误以为公开数据不存在隐私问题。|
|25|D|一句话解析：A 以为用了抽象就不必写用户说明，B 把过程抽象误当成模拟，C 编造了参数传递对变量名的要求。|
