# CSA 原创选择题 61–120

61. <code>int[] a={2,4,6}; int s=0; for(int i=0;i<a.length;i++) s+=a[i];</code> 后 s？ A. 6 B. 10 C. 12 D. 24
62. <code>int[] a={1,2,3,4}; for(int i=0;i<a.length/2;i++){int t=a[i];a[i]=a[a.length-1-i];a[a.length-1-i]=t;}</code> 后 a？ A. {4,3,2,1} B. {2,1,4,3} C. {1,2,3,4} D. {4,2,3,1}
63. <code>int[] a={3,1,3,3}; int c=0; for(int x:a) if(x==3)c++;</code> 后 c？ A. 1 B. 2 C. 3 D. 4
64. 哪个循环能访问每对相邻元素 a[i-1]、a[i]？ A. i 从 0 B. i 从 1 到 length-1 C. i 从 length D. 只用增强 for
65. <code>int[] a={5};</code> 执行 <code>a[a.length]</code> 会？ A. 返回 5 B. 返回 0 C. 运行时越界 D. 编译错误
66. <code>ArrayList<Integer> v=[2,4,6]</code>（简写），执行 <code>v.add(1,9)</code> 后？ A. [2,9,4,6] B. [2,9,6] C. [9,2,4,6] D. [2,4,6,9]
67. 上题结果再执行 <code>v.remove(2)</code> 后？ A. [2,4,6] B. [2,9,6] C. [2,9,4] D. [9,4,6]
68. <code>ArrayList<String> x</code> 为空时，哪个调用合法？ A. <code>x.get(0)</code> B. <code>x.set(0,"a")</code> C. <code>x.add("a")</code> D. <code>x.remove(0)</code>
69. 删除 list 中所有偶数，最稳妥的索引方向？ A. 从后向前 B. 从前向后且总 i++ C. 随机 D. 增强 for 中 remove
70. <code>for(int i=0;i<list.size();i++) if(list.get(i)<0) list.remove(i);</code> 可能出现？ A. 索引越界异常 B. 跳过相邻负数 C. 编译错误 D. 列表不变
71. <code>int[][] g={{1,2},{3,4},{5,6}};</code> 所有元素总和？ A. 15 B. 18 C. 21 D. 24
72. 上题 <code>g[2][0]</code> 是？ A. 3 B. 4 C. 5 D. 6
73. <code>for(int[] row:g) for(int x:row) System.out.print(x);</code> 对上题输出？ A. 123456 B. 135246 C. 654321 D. 132435
74. 对可能不规则二维数组，内层循环上界应优先用？ A. <code>g.length</code> B. <code>g[0].length</code> C. <code>g[r].length</code> D. 固定 10
75. 一个 4×5 数组按行完整遍历，内层语句执行次数？ A. 9 B. 16 C. 20 D. 25
76. <code>Integer.parseInt("27")</code> 返回？ A. String "27" B. int 27 C. double 27.0 D. Integer 数组
77. <code>Double.parseDouble("2.5")</code> 返回类型？ A. int B. double C. boolean D. String
78. autoboxing 示例是？ A. Integer 到 int B. int 自动变 Integer C. String 到 int D. double 到 String
79. 若 <code>Integer x=null; int y=x;</code>，最可能？ A. y=0 B. y=-1 C. 运行时错误 D. 编译错误
80. Scanner 逐个读取整数直到文件结束，循环条件最合适？ A. <code>hasNextInt()</code> B. <code>nextInt()</code> C. <code>hasNextLine()==false</code> D. <code>length()>0</code>
81. <code>nextInt()</code> 后立即 <code>nextLine()</code> 常见问题？ A. 文件被删除 B. nextLine 可能读到剩余换行 C. 整数翻倍 D. 编译器退出
82. 下列哪项是文本文件使用后的正确动作？ A. 调用 close B. 把 Scanner 设为 int C. 排序文件名 D. 调用 Math.random
83. 线性搜索数组 [8,3,5,2] 查找 5，从左开始共比较？ A. 1 B. 2 C. 3 D. 4
84. 二分查找 [2,4,6,8,10,12,14] 首先检查中间值通常是？ A. 2 B. 6 C. 8 D. 14
85. 二分查找上题中的 12，检查 8 后下一候选范围？ A. [2,4,6] B. [10,12,14] C. [8] D. 全部
86. 对无序数组直接二分查找的主要问题？ A. 不能编译 B. 排除一半的判断不可靠 C. 一定更慢 D. 只能找偶数
87. 选择排序升序处理 [4,1,3] 第一轮后？ A. [1,4,3] B. [4,1,3] C. [3,1,4] D. [1,3,4]
88. 插入排序升序处理 [2,5,3,1]，把 3 插入已排序前缀后？ A. [2,3,5,1] B. [3,2,5,1] C. [1,2,3,5] D. [2,5,1,3]
89. 归并两个有序序列 [1,4,7] 和 [2,3,8] 的结果？ A. [1,2,3,4,7,8] B. [1,4,7,2,3,8] C. [2,3,8,1,4,7] D. [1,2,4,3,7,8]
90. 选择排序 n 个元素的比较次数增长通常最接近？ A. 常数 B. 线性 C. 平方 D. 对数
91. 方法 <code>f(n){if(n==0)return 1; return n*f(n-1);}</code>，f(4)？ A. 10 B. 16 C. 24 D. 120
92. <code>p(n){if(n<=0)return; print(n);p(n-2);}</code>，p(5) 输出？ A. 531 B. 135 C. 54321 D. 53
93. 若把上题 print 放到递归调用之后，p(5) 输出？ A. 531 B. 135 C. 54321 D. 13
94. 递归调用参数始终不变且 base case 未满足，结果通常？ A. 正常返回 0 B. 栈溢出 C. 无限循环但只打印一次 D. 编译时就会被检测并报错
95. 二分查找递归每次把问题规模约变为？ A. n-1 B. n/2 C. n² D. 2n
96. 归并排序递归拆分何时停止？ A. 子序列长度为 1 B. 出现负数 C. 数组已满 D. 随机值为 0
97. 类中 <code>private int count;</code> 属于？ A. 参数 B. 实例变量 C. 局部常量 D. 方法
98. 构造器 <code>public Counter(int start){count=start;}</code> 的作用？ A. 返回 start B. 初始化新对象字段 C. 创建静态方法 D. 删除对象
99. 若方法声明 <code>public int get()</code>，哪条方法体合法？ A. <code>return count;</code> B. <code>return;</code> C. <code>System.out.print(count);</code> 且无 return D. <code>return true;</code>
100. <code>public void set(int x){value=x;}</code> 属于？ A. accessor B. mutator C. constructor D. class header
101. 两个 Counter 对象的实例变量 count 通常？ A. 必须相同 B. 各自一份 C. 自动相加 D. 属于 Math
102. 若 count 声明 static，则两个对象访问的是？ A. 各自副本 B. 同一个共享字段 C. null D. 局部变量
103. 哪项违反封装的风险最大？ A. private 字段配 getter B. public 可变实例字段 C. 构造器初始化 D. 短方法
104. 方法局部变量在方法结束后？ A. 仍能由其他类直接使用 B. 超出作用域 C. 自动成为字段 D. 变 static
105. <code>public Thing getSelf(){return this;}</code> 返回？ A. 新对象 B. 当前对象引用 C. null D. 类名
106. 对对象参数 p 执行 <code>p.setValue(9)</code> 可能怎样？ A. 调用者看到对象状态改变 B. 只改局部 int C. 必然编译错误 D. p 自动变 null
107. 方法内执行 <code>p=new Thing();</code> 对调用者的引用变量本身？ A. 必然改指向新对象 B. 不会改其保存的引用值 C. 被删除 D. 变 static
108. String 作为参数时，在方法内 <code>s=s+"!"</code> 对调用者 String 变量？ A. 自动更新 B. 通常不改调用者变量 C. 修改原 String 对象 D. 编译错误
109. 哪个最能体现过程抽象？ A. 把重复计算封装成带参数方法 B. 复制相同代码五次 C. 所有字段 public D. 删除方法名
110. 方法只承诺对非空数组工作，“非空”是？ A. 后置条件 B. 前置条件 C. 编译器选项 D. 返回类型
111. 算法处理完后保证数组升序，“升序”是？ A. 后置条件 B. 参数 C. 包名 D. 构造器
112. 数据集只采集城市智能手机用户，推断全国人口时主要风险？ A. 溢出 B. 样本偏差 C. 语法错误 D. 递归
113. 把姓名和精确位置长期保存主要涉及？ A. 数据隐私 B. 方法重载 C. 排序稳定性 D. 整数除法
114. 相关性很强能否单独证明因果？ A. 总能 B. 不能 C. 仅在数组中能 D. 编译后能
115. 删除姓名后数据一定匿名吗？ A. 是 B. 否，其他字段组合可能重新识别 C. 只要排序就匿名 D. 只有 int 数据匿名
116. 哪个测试集最全面测试方法是否接受 1–10？ A. 只测 5 B. 测 1、10、0、11 和中间值 C. 只测 1 D. 只测 10
117. 若要找最后一个匹配索引，哪种做法正确？ A. 首次匹配立即 return B. 遍历完并持续更新结果 C. 只查中间 D. 先删除全部
118. 数组右移一位时，为防覆盖，通常应？ A. 从右向左复制 B. 从左向右复制 C. 只复制首项 D. 使用增强 for
119. 原地计算每格“原值+左邻居”且从左到右更新，主要风险？ A. 读到已修改左邻居 B. 数组自动清空 C. 无法访问索引 D. Java 自动回滚
120. 当前 CSA 选择题占考试成绩？ A. 30% B. 45% C. 55% D. 70%
