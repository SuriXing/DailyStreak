// AUTO-GENERATED from the AP CSA · 情境题 materials (25 cards). Do not hand-edit.
// Regenerate: node scripts/build-subject-decks.js
export type SubjectDeckKey = 'csa-scenario';

export interface SubjectFlashcard {
  id: string;
  deck: SubjectDeckKey;
  category: string;
  front: string;
  back: string;
  /** 文本来源：ai-mcq = AI 生成的原创选择题，不是 College Board 真题 */
  source: 'ai-mcq';
  /** 是否经过逐题独立验算/事实核查 */
  verified: boolean;
}

export const SUBJECT_DECKS_CSA_SCENARIO: { key: SubjectDeckKey; label: string }[] = [
  { key: "csa-scenario", label: "AP CSA · 情境题" },
];

export const SUBJECT_FLASHCARDS_CSA_SCENARIO: SubjectFlashcard[] = [
  { id: "csa-scenario-001", deck: "csa-scenario", category: "AP CSA · 情境题", front: "ArrayList<Integer> a = new ArrayList<Integer>();\nint[] d = {2, 4, 5, 6, 8};\nfor (int v : d) { a.add(v); }\nfor (int i = 0; i < a.size(); i++) { if (a.get(i) % 2 == 0) { a.remove(i); } }\nSystem.out.println(a); 的输出，以及让\"删除全部偶数\"成立的循环写法是？\nA. 输出 [4, 5, 8]；换成按元素删除的 remove 重载即可删净\nB. 输出 [5]；认为改用增强 for 遍历并删除就能删净全部偶数\nC. 输出 [4, 5, 8]；改为从末尾向前遍历删除才能删净所有偶数\nD. 输出 [5]；认为原循环已经正确删掉全部偶数，不需要修改", back: "答案：C\n简析：一句话解析：A 换成按元素删除的重载后下标仍会前进，被左移的 4 被跳过；B 在增强 for 中删除会抛 ConcurrentModificationException；D 实际输出会留下偶数 8，说明并未删净。\n\n正确选项：输出 [4, 5, 8]；改为从末尾向前遍历删除才能删净所有偶数", source: "ai-mcq", verified: true },
  { id: "csa-scenario-002", deck: "csa-scenario", category: "AP CSA · 情境题", front: "static void modify(ArrayList<Integer> p) {\np.add(0, 99);\np = new ArrayList<Integer>();\np.add(7);\n}\nArrayList<Integer> a = new ArrayList<Integer>();\na.add(1);\na.add(2);\nmodify(a);\nSystem.out.println(a);\nA. 输出 [99, 1, 2]；p 与 a 指向同一列表，重新赋值只改本地引用\nB. 输出 [99, 1, 2, 7]；p 始终是 a 的别名，两次 add 都写进同一个列表\nC. 输出 [1, 2]；认为方法内对参数引用的修改不会传回调用者的列表\nD. 输出 [7, 99, 1, 2]；p = new 相当于清空 a 之后再插入 7", back: "答案：A\n简析：一句话解析：B 误以为 p 重新赋值后仍指向 a；C 忽略了 p.add(0, 99) 已经改动了 a；D 把 p = new 误解成清空原列表再插入。\n\n正确选项：输出 [99, 1, 2]；p 与 a 指向同一列表，重新赋值只改本地引用", source: "ai-mcq", verified: true },
  { id: "csa-scenario-003", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int[][] t = {{5, 1}, {2, 7, 3}};\nint s = 0;\nfor (int r = 0; r < t.length; r++) {\nfor (int c = 0; c <= t[r].length; c++) {\ns += t[r][c];\n}\n}\nSystem.out.println(s); 的运行结果是？\nA. 输出 18；越界位置读到的默认值 0 不会影响总和\nB. 输出 15；内层条件等价于按第一行的长度遍历两行\nC. 编译错误；不规则的二维数组初始化在 Java 中不合法\nD. 运行时抛出越界异常；r=0 时 c 取到非法下标 2", back: "答案：D\n简析：一句话解析：A 越界会直接抛异常，读不到默认值 0；B 内层条件是 c <= t[r].length，并不会按第一行的长度截断；C 锯齿数组的这种初始化在 Java 中完全合法。\n\n正确选项：运行时抛出越界异常；r=0 时 c 取到非法下标 2", source: "ai-mcq", verified: true },
  { id: "csa-scenario-004", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int[][] g = new int[2][2];\nint[] row = g[0];\nrow[0] = 8;\ng[1] = row;\nrow[1] = 9;\nSystem.out.println(g[0][0] + \" \" + g[1][1]);\nA. 输出 0 0；数组之间的赋值都会复制出一份新数组\nB. 输出 8 9；两行是同一个数组，row 的改动两处可见\nC. 输出 0 9；row 是复制出来的行，不再关联 g[0] 这一行\nD. 输出 8 0；g[1] = row 复制了当时的内容，之后互不影响", back: "答案：B\n简析：一句话解析：A 数组赋值不复制元素，两个变量会指向同一数组；C 忽略了 int[] row = g[0] 赋的是引用；D 忽略了 g[1] = row 之后两行共享同一数组。\n\n正确选项：输出 8 9；两行是同一个数组，row 的改动两处可见", source: "ai-mcq", verified: true },
  { id: "csa-scenario-005", deck: "csa-scenario", category: "AP CSA · 情境题", front: "static void fill(int[] arr) {\nfor (int i = 0; i < arr.length; i++) { arr[i] = arr[i] + 1; }\n}\nstatic void replace(int[] arr) { arr = new int[]{0, 0, 0}; }\nint[] a = {1, 2, 3};\nfill(a);\nreplace(a);\nSystem.out.println(a[0] + \",\" + a[2]);\nA. 输出 2,4；fill 改了数组，replace 只改本地引用\nB. 输出 0,0；认为 replace 里新建的数组也赋给了调用者的 a\nC. 输出 1,3；方法内对数组元素的修改不会影响调用者的数组\nD. 编译错误；方法参数被隐式声明为 final，不允许重新赋值", back: "答案：A\n简析：一句话解析：B replace 只改了形参引用，没有写回 a；C fill 是通过引用修改元素的，会影响调用者的数组；D Java 方法参数不是 final，重新赋值完全合法。\n\n正确选项：输出 2,4；fill 改了数组，replace 只改本地引用", source: "ai-mcq", verified: true },
  { id: "csa-scenario-006", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int[] a = {3, 1, 4};\nfor (int x : a) { x = x * 2; }\n学生本想让每个元素翻倍。关于数组最终内容和正确改法，哪项正确？\nA. {6, 2, 8}；认为 x 是数组元素的别名，循环结束时已全部翻倍\nB. {3, 1, 4}；应改成 a[x] = x * 2，因为把 x 当成了元素的下标\nC. 编译错误；认为增强 for 的循环变量在循环体内不能被重新赋值\nD. {3, 1, 4}；x 只是副本，须改用 a[i] = a[i] * 2", back: "答案：D\n简析：一句话解析：A 增强 for 的 x 与数组元素没有引用关系；B x 是元素值而不是下标，a[x] 会越界；C 这段代码能正常编译，只是不会改到数组。\n\n正确选项：{3, 1, 4}；x 只是副本，须改用 a[i] = a[i] * 2", source: "ai-mcq", verified: true },
  { id: "csa-scenario-007", deck: "csa-scenario", category: "AP CSA · 情境题", front: "ArrayList<String> w = new ArrayList<String>();\nw.add(\"a\");\nw.add(\"bb\");\nw.add(\"ccc\");\nArrayList<String> r = new ArrayList<String>();\nfor (String s : w) { if (s.length() > 1) { r.add(0, s); } }\nSystem.out.println(r);\nA. [bb, ccc]；把 add(0, s) 当成在列表末尾追加元素\nB. [ccc, bb]；只保留长度大于 1 的串，且每次插到索引 0\nC. [ccc]；把 add(0, s) 当成替换索引 0 处的元素\nD. 抛出越界异常；认为空列表上在索引 0 处插入是非法操作", back: "答案：B\n简析：一句话解析：A add(0, s) 是插入而不是末尾追加；C add 不是 set，不会替换已有元素；D 在空列表索引 0 处插入是合法的，不会抛异常。\n\n正确选项：[ccc, bb]；只保留长度大于 1 的串，且每次插到索引 0", source: "ai-mcq", verified: true },
  { id: "csa-scenario-008", deck: "csa-scenario", category: "AP CSA · 情境题", front: "class Animal { public String speak() { return \"?\"; } }\nclass Dog extends Animal { public String speak() { return \"woof\"; } public String fetch() { return \"ball\"; } }\nAnimal a = new Dog();\nDog d = (Dog) a;\nSystem.out.println(a.speak() + d.fetch());\nA. ?ball；认为变量 a 的声明类型就决定了最终调用哪个 speak 版本\nB. 编译错误；认为父类引用必须先向下转型才能调用 speak 方法\nC. woofball；按运行时对象绑定 speak，转型后可调 fetch\nD. 运行时抛类型转换异常；认为父类引用转成子类时一定会失败", back: "答案：C\n简析：一句话解析：A 被覆盖的方法按运行时类型选版本，与声明类型无关；B 父类引用可以直接调用 speak，不需要先转型；D 实际对象就是 Dog，向下转型不会失败。\n\n正确选项：woofball；按运行时对象绑定 speak，转型后可调 fetch", source: "ai-mcq", verified: true },
  { id: "csa-scenario-009", deck: "csa-scenario", category: "AP CSA · 情境题", front: "class Shape { public double area() { return 0; } public String name() { return \"shape\"; } }\nclass Square extends Shape { public double area() { return 4; } }\nclass Circle extends Shape { public double area() { return 3; } public String name() { return \"circle\"; } }\nShape[] s = { new Square(), new Circle(), new Shape() };\ndouble t = 0;\nfor (Shape p : s) { t += p.area(); }\nSystem.out.println(t + \" \" + s[1].name());\nA. 7.0 circle；area 按运行时对象调用，name 也被覆盖\nB. 0.0 circle；Shape 变量只会调用父类定义的 area 实现\nC. 7.0 shape；name 没有被覆盖，所以输出父类里定义的字符串\nD. 运行时抛类型转换异常；因为遍历异构数组时都要向下转型", back: "答案：A\n简析：一句话解析：B 动态绑定会调用子类覆盖后的 area；C name 已在 Circle 中被覆盖；D 父类引用调用被覆盖的方法并不需要向下转型。\n\n正确选项：7.0 circle；area 按运行时对象调用，name 也被覆盖", source: "ai-mcq", verified: true },
  { id: "csa-scenario-010", deck: "csa-scenario", category: "AP CSA · 情境题", front: "public class Ticket {\nprivate static int sold = 0;\nprivate int id;\npublic Ticket() { sold++; id = sold; }\npublic int getId() { return id; }\npublic static int getSold() { return sold; }\n}\nTicket a = new Ticket();\nTicket b = new Ticket();\nSystem.out.println(a.getId() + \" \" + b.getId() + \" \" + Ticket.getSold());\nA. 0 1 2；先执行 id = sold 再执行 sold++，编号从 0 开始\nB. 2 2 2；认为 id 也声明为 static，两个对象共用同一编号\nC. 编译错误；认为构造方法体内不能访问类的静态字段 sold\nD. 1 2 2；sold 被所有对象共享，每次构造先自增再赋给 id", back: "答案：D\n简析：一句话解析：A 代码先自增再赋值，编号从 1 开始；B id 是实例字段，每个对象各有一份；C 在构造方法里访问静态字段完全合法。\n\n正确选项：1 2 2；sold 被所有对象共享，每次构造先自增再赋给 id", source: "ai-mcq", verified: true },
  { id: "csa-scenario-011", deck: "csa-scenario", category: "AP CSA · 情境题", front: "class P {\npublic P() { System.out.print(\"P\"); }\npublic P(int x) { System.out.print(\"P\" + x); }\n}\nclass C extends P {\npublic C() { super(2); System.out.print(\"C\"); }\n}\nnew C();\nA. PC；因为 super(2) 找不到匹配，退回调用父类无参构造器\nB. P2C；先执行父类带参构造器输出 P2，再执行子类体输出 C\nC. CP2；认为子类构造器体先执行完，再执行父类的构造器\nD. 编译错误；认为子类构造器只能通过 super() 调用无参构造器", back: "答案：B\n简析：一句话解析：A 显式 super(2) 能匹配到父类带参构造器，不会退回无参版本；C 父类构造器一定先于子类构造器体执行；D 子类构造器可以调用父类的带参构造器。\n\n正确选项：P2C；先执行父类带参构造器输出 P2，再执行子类体输出 C", source: "ai-mcq", verified: true },
  { id: "csa-scenario-012", deck: "csa-scenario", category: "AP CSA · 情境题", front: "abstract class Food {\npublic abstract double cost();\n}\nclass Pizza extends Food {\nprivate int slices;\npublic Pizza(int n) { slices = n; }\npublic double cost(int n) { return n * 3.0; }\n}\nFood f = new Pizza(4);\nSystem.out.println(f.cost());\nA. 运行时抛 AbstractMethodError；f.cost() 找不到实现\nB. 编译错误；认为错在调用处，必须先把 f 转型为 Pizza 才行\nC. 编译错误；cost(int) 是重载不是覆盖，应改成无参的 cost()\nD. 无错误；认为方法名相同就算实现了抽象方法，输出 12.0", back: "答案：C\n简析：一句话解析：A 抽象方法未实现是编译期错误，不会拖到运行时；B 错误出在 Pizza 的类声明上，转型也解决不了；D 方法名相同不等于覆盖，参数列表不同只是重载。\n\n正确选项：编译错误；cost(int) 是重载不是覆盖，应改成无参的 cost()", source: "ai-mcq", verified: true },
  { id: "csa-scenario-013", deck: "csa-scenario", category: "AP CSA · 情境题", front: "public static int f(int n) {\nif (n <= 1) { return n; }\nreturn f(n - 1) + f(n - 2);\n}\n调用 f(4) 后，返回值与 f 被调用的总次数（含初始调用）分别是？\nA. 返回值 3，f 共被调用 9 次，含最初的 f(4) 调用\nB. 返回值 5，f 共被调用 9 次，因为把 n <= 1 都当成返回 1\nC. 返回值 3，f 共被调用 8 次，因为没把初始调用计入次数\nD. 返回值 3，f 共被调用 7 次，因为漏数了两次 f(0) 调用", back: "答案：A\n简析：一句话解析：B 基础情形的返回值并非都是 1，f(0) 返回 0；C 初始调用 f(4) 也要计入总次数；D f(0) 确实被调用了两次，不能漏数。\n\n正确选项：返回值 3，f 共被调用 9 次，含最初的 f(4) 调用", source: "ai-mcq", verified: true },
  { id: "csa-scenario-014", deck: "csa-scenario", category: "AP CSA · 情境题", front: "public static String g(String s) {\nif (s.length() <= 1) { return s; }\nreturn g(s.substring(1)) + s.charAt(0);\n}\ng(\"abc\") 的返回值是？\nA. bca；只完成最外层一次拼接，没有继续在子串上递归\nB. abc；把拼接顺序记反，写成 charAt(0) + g(s)\nC. cba；每层先反转剩余子串，再把首字符接到结果末尾\nD. 抛出越界异常；认为空串上调用 charAt(0) 也会越界失败", back: "答案：C\n简析：一句话解析：A 代码会继续在子串上递归，不会只做最外层一次；B 拼接顺序是递归结果在前，不是 charAt(0) 在前；D 基础情形拦住了空串，charAt(0) 不会被调用。\n\n正确选项：cba；每层先反转剩余子串，再把首字符接到结果末尾", source: "ai-mcq", verified: true },
  { id: "csa-scenario-015", deck: "csa-scenario", category: "AP CSA · 情境题", front: "public static int h(int n) {\nif (n == 0) { return 0; }\nreturn n + h(n - 2);\n}\nh(5) 的结果是？\nA. 返回 9；认为 n 递减到 1 时会自动满足终止条件\nB. 抛出栈溢出错误；奇数 n 跳过 0，递归永远不会终止\nC. 返回 15；忽略步长 2，把 h 当成计算 1 到 5 的连续整数和\nD. 返回 8；认为最后剩下的 h(1) 不计入结果，只有 5 与 3 相加", back: "答案：B\n简析：一句话解析：A n == 0 是唯一终止条件，奇数序列取不到 0；C 步长是 2，并不是逐个累加 1 到 5；D 递归根本不会结束，不存在剩下 h(1) 的结果。\n\n正确选项：抛出栈溢出错误；奇数 n 跳过 0，递归永远不会终止", source: "ai-mcq", verified: true },
  { id: "csa-scenario-016", deck: "csa-scenario", category: "AP CSA · 情境题", front: "public static int m(int[] a, int i) {\nif (i == a.length) { return 0; }\nint rest = m(a, i + 1);\nreturn a[i] > rest ? a[i] : rest;\n}\nint[] d = {3, 9, 4};\nint r = m(d, 0); // r 的值是？\nA. 0；认为三元表达式最终返回的是基础情形的值\nB. 3；认为没有比较过程，直接返回第一个元素 a[0] 的值\nC. 4；认为递归返回的总是数组最后一个元素的值\nD. 9；逐层把当前元素与右侧子问题的最大值比较后返回", back: "答案：D\n简析：一句话解析：A 基础情形只作为比较初值，最终返回的是最大值；B 每层都做了比较，不是直接返回 a[0]；C 返回值由比较结果决定，与最后一个元素无关。\n\n正确选项：9；逐层把当前元素与右侧子问题的最大值比较后返回", source: "ai-mcq", verified: true },
  { id: "csa-scenario-017", deck: "csa-scenario", category: "AP CSA · 情境题", front: "public static void p(int n) {\nif (n == 0) { return; }\np(n - 1);\nSystem.out.print(n);\np(n - 1);\n}\np(2); 的输出是？\nA. 211；把打印语句放在两次递归调用之前，先打印再递归\nB. 121；先走左侧递归到基础情形，再打印 n，再走右侧递归\nC. 12；漏掉最后一行 p(n - 1) 调用，只走了左半边的递归\nD. 112；把打印语句放在两次递归调用之后，两侧递归完才打印", back: "答案：B\n简析：一句话解析：A 打印写在递归之前会先输出 2；C 漏掉最后一次 p(n - 1) 会少输出一个 1；D 打印写在两次递归之后会得到 112。\n\n正确选项：121；先走左侧递归到基础情形，再打印 n，再走右侧递归", source: "ai-mcq", verified: true },
  { id: "csa-scenario-018", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int[] a = {5, 3, 8, 1, 9};\nfor (int i = 0; i < 2; i++) {\nint min = i;\nfor (int j = i + 1; j < a.length; j++) { if (a[j] < a[min]) { min = j; } }\nint t = a[i]; a[i] = a[min]; a[min] = t;\n}\n循环结束后数组的内容，以及要完全升序还需的交换次数是？\nA. {1, 3, 8, 5, 9}，还需 1 次交换；i=2 时把 5 换到索引 2\nB. {1, 3, 8, 5, 9}，还需 2 次交换；认为剩余两趟都必然要交换\nC. {1, 3, 5, 8, 9}，还需 0 次交换；认为两趟已排好前三个位置\nD. {1, 1, 8, 1, 9}，还需 1 次交换；认为只用赋值把最小值搬到前面即可", back: "答案：A\n简析：一句话解析：B 剩余两趟里只有 i=2 需要交换；C 两趟只固定了前两个位置，数组并未有序；D 代码用的是三变量交换，不会用赋值覆盖掉元素 5。\n\n正确选项：{1, 3, 8, 5, 9}，还需 1 次交换；i=2 时把 5 换到索引 2", source: "ai-mcq", verified: true },
  { id: "csa-scenario-019", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int[] a = {2, 4, 6, 8, 10, 12, 14};\n// 二分查找 12：lo=0，hi=a.length-1，mid=(lo+hi)/2，\n// 每次把 a[mid] 与目标比较，命中即停止\n依次得到的 mid 下标和总比较次数分别是？\nA. 3、4、5，共 3 次；把 lo 写成 mid 而不是 mid + 1，多走一趟\nB. 3、5，共 3 次；命中 12 后再与相邻元素比较一次作确认\nC. 4、6，共 2 次；把下标按 1 开始编号，(1 + 7) / 2 得 4\nD. 3、5，共 2 次；先比 a[3]=8 再比 a[5]=12，两次命中", back: "答案：D\n简析：一句话解析：A 把 lo 写成 mid 会多比较一趟，mid 会多出 4；B 命中后立即停止，不需要再比较相邻元素；C 数组下标从 0 开始，mid 不可能是 4、6。\n\n正确选项：3、5，共 2 次；先比 a[3]=8 再比 a[5]=12，两次命中", source: "ai-mcq", verified: true },
  { id: "csa-scenario-020", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int[] a = {4, 2, 7, 1};\n// 插入排序：外层 i 从 1 到 a.length-1，把 a[i] 插入有序前缀，\n// 每个被向后挪一位的元素记 1 次右移\n执行完 i = 2 这一趟后的数组内容，以及整个排序的右移总次数是？\nA. {1, 2, 4, 7}，共 4 次右移；认为 i=2 结束后数组已完全有序\nB. {2, 4, 1, 7}，共 4 次右移；认为每趟都与右侧相邻元素交换位置\nC. {2, 4, 7, 1}，共 4 次右移；i=2 时 7 已在位，只有 1 需要移动\nD. {2, 4, 7, 1}，共 5 次右移；把比较次数当成右移次数（1+1+3=5），混淆比较与移动", back: "答案：C\n简析：一句话解析：A 执行完 i=2 时 1 还没被插入，数组并未有序；B 插入排序是右移腾位，不是交换相邻元素；D 右移次数是 1+0+3=4，5 是比较次数。\n\n正确选项：{2, 4, 7, 1}，共 4 次右移；i=2 时 7 已在位，只有 1 需要移动", source: "ai-mcq", verified: true },
  { id: "csa-scenario-021", deck: "csa-scenario", category: "AP CSA · 情境题", front: "public static int count(int[] a, int t) {\nint c = 0;\nfor (int i = 0; i < a.length; i++) {\nif (a[i] == t) { c++; return c; }\n}\nreturn c;\n}\nint[] d = {3, 5, 3, 5};\ncount(d, 3) 的返回值与正确的修改是？\nA. 返回 1；与预期 2 不符，应把 return c 移到循环外部\nB. 返回 2，与预期一致；循环会继续比较后面的元素并累加计数\nC. 编译错误；认为存在没有 return 的执行路径，所以方法缺少返回值\nD. 返回 1；应把 c++ 改成 c = c + 1 让自增提前生效", back: "答案：A\n简析：一句话解析：B return 写在循环体内，命中第一次就结束方法；C 方法末尾已有 return，能通过编译；D c++ 自增是生效的，真正的问题是提前返回。\n\n正确选项：返回 1；与预期 2 不符，应把 return c 移到循环外部", source: "ai-mcq", verified: true },
  { id: "csa-scenario-022", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int a = 7, b = 2;\ndouble r = a / b;\ndouble s = (double) a / b;\nSystem.out.println(r + \" \" + s);\nA. 3.5 3.5；认为把结果赋给 double 会让右侧先按 double 运算\nB. 3.0 3.5；第一行先做整数除法再拓宽，第二行强制转换后得小数\nC. 3 3.5；认为 double 变量存整数值时打印结果不带小数点\nD. 3.5 3.0；把 (double) 的作用位置记反，以为只有第一行被提升", back: "答案：B\n简析：一句话解析：A 赋值目标的类型不会影响右侧的整数除法；C double 变量打印整数值时仍带小数点；D 提升只发生在第二行，第一行依旧是整数除法。\n\n正确选项：3.0 3.5；第一行先做整数除法再拓宽，第二行强制转换后得小数", source: "ai-mcq", verified: true },
  { id: "csa-scenario-023", deck: "csa-scenario", category: "AP CSA · 情境题", front: "char c = 'A';\nint k = c + 1;\nchar d = (char) k;\nSystem.out.println(k + \" \" + d);\nA. 66 A；认为 (char) 窄化转换不改变字符，d 仍是 'A'\nB. 66 66；认为 char 变量用 println 输出时会显示编码数字\nC. 66 B；'A' 编码为 65，加 1 后转回 char 是 B\nD. 编译错误；认为 char 与 int 做算术运算时必须先显式转成 int", back: "答案：C\n简析：一句话解析：A (char) 确实把 66 转成了字符 B；B println 打印 char 变量输出的是字符本身；D char 与 int 混合运算会自动提升，不需要显式转换。\n\n正确选项：66 B；'A' 编码为 65，加 1 后转回 char 是 B", source: "ai-mcq", verified: true },
  { id: "csa-scenario-024", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int i = 0;\nboolean b = (i++ > 0) && (i++ > 1);\nSystem.out.println(i + \" \" + b);\nA. 2 false；认为 && 两侧都会被求值，两次自增都会执行\nB. 2 true；把后缀 i++ 当成前缀 ++i，于是两个条件都成立\nC. 0 false；认为整个布尔表达式失败时其中的自增都不生效\nD. 1 false；左侧为 false 短路，第二次自增不再执行", back: "答案：D\n简析：一句话解析：A && 左侧为 false 时右侧根本不求值；B i++ 先用旧值 0 比较，左侧是 false；C 左侧的 i++ 已经执行，i 不会回到 0。\n\n正确选项：1 false；左侧为 false 短路，第二次自增不再执行", source: "ai-mcq", verified: true },
  { id: "csa-scenario-025", deck: "csa-scenario", category: "AP CSA · 情境题", front: "int i = 0, sum = 0;\nwhile (i < 5) {\ni++;\nif (i % 2 == 0) { continue; }\nsum += i;\n}\nSystem.out.println(sum + \" \" + i);\nA. 9 5；只累加奇数 1、3、5，i 到 5 后退出循环\nB. 15 5；忽略 if 与 continue，把 1 到 5 全部累加\nC. 1 2；把 continue 当成 break，遇到偶数就退出循环\nD. 6 5；把取余条件看反，只把偶数 2 与 4 累加进去", back: "答案：A\n简析：一句话解析：B continue 会跳过偶数，不是把 1 到 5 全加；C continue 只跳过本轮，不会结束循环；D 条件是偶数时跳过，累加的是奇数。\n\n正确选项：9 5；只累加奇数 1、3、5，i 到 5 后退出循环", source: "ai-mcq", verified: true },
];
