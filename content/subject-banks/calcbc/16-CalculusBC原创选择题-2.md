# AP Calculus BC 原创选择题 2（61–120）

AI 原创仿 AP 题，不是 College Board 真题。

## 微分方程与几何应用

61. Euler 法一步从(x0,y0)步长h的结果是？ A `y0+h f(x0,y0)` B `y0+f(x0+h,y0)` C `f(x0,y0+h)` D `y0/h`
62. `dy/dx=3y,y(0)=2` 的解？ A `2+3x` B `2e^{3x}` C `3e^{2x}` D `2e^x+3`
63. `dy/dx=x/y` 分离后？ A `y dy=x dx` B `dy/y=x dx` C `x dy=y dx` D `y dx=x dy`
64. Logistic `P'=kP(1-P/L)` 的平衡解？ A 仅0 B 仅L C 0和L D L/2
65. Logistic 增长率最大通常在？ A P=0 B P=L/2 C P=L D P=2L
66. 斜率场中同一水平线上的斜率相同，方程最可能？ A y'=f(x) B y'=g(y) C y'=xy D y'=x+y
67. 验证候选解至少需检查？ A 仅初值 B 仅微分方程 C 两者 D 图像
68. 区域在y=f与y=g间、f≥g，绕x轴且均非负，垫片体积？ A `π∫(f-g)²` B `π∫(f²-g²)` C `2π∫x(f-g)` D `∫(f-g)`
69. 绕y轴用竖直壳，半径通常是？ A y B x C f' D f²
70. 底面截面边长为s(x)的正方形，体积？ A `∫s dx` B `π∫s²` C `∫s² dx` D `∫sqrt(s)dx`
71. y=f与y=g间面积必须？ A `∫(f-g)`，不区分谁在上 B 积分非负的上减下并按交点分段 C 只需先求出两条曲线的交点 D 最后把面积再乘以 π 得体积
72. `y=x²` 从0到1的弧长表达式？ A `∫_0^1sqrt(1+4x²)dx` B `∫_0^1sqrt(1+(2x²)²)dx` C `∫_0^1sqrt(1+x²)dx` D `∫_0^1(1+(2x)²)dx`
73. `∫_0^1 1/sqrt(x) dx`？ A 发散 B 收敛到1 C 收敛到2 D 收敛到∞
74. `∫_{-1}^1 1/x² dx`？ A 0 B 2 C 收敛 D 发散
75. 流入R、流出S，初量Q0，则Q(t)=? A `Q0+∫(R-S)` B `Q0+R-S` C `∫(R+S)` D `Q0-∫R`
76. 若R(t)>S(t)，库存量在该时刻？ A 增加 B 减少 C 为0 D 无法定义
77. 速度变号点对总路程计算的作用？ A 可忽略 B 必须作为分段点 C 只算加速度 D 改用平均速度
78. `dy/dx=y(4-y)` 在0<y<4时解曲线？ A 递减 B 递增 C 水平 D 不存在
79. 上式 y>4时？ A y'>0 B y'<0 C y'=0 D 符号未知
80. Euler 法通常步长更小会？ A 结果与解析解完全一致 B 通常提高近似精度 C 改变微分方程 D 使局部误差单调增大

## 参数、极坐标与向量

81. x=t²+1,y=t³，则 dy/dx=? A `3t²/(2t)` B `2t/(3t²)` C `3t²` D `2t`
82. 上题t=1时斜率？ A 2/3 B 1 C 3/2 D 3
83. 参数曲线二阶导数应将 `d/dt(dy/dx)` 再除以？ A dy/dt B dx/dt C d²x/dt² D t
84. r(t)=<t²,sint> 的速度？ A <2t,cost> B <t²,cost> C <2,sint> D <2t,-sint>
85. 上题速率？ A `2t+cost` B `sqrt(4t²+cos²t)` C `4t²+cos²t` D `sqrt(t⁴+sin²t)`
86. 极面积公式？ A `∫r dθ` B `π∫r²dθ` C `(1/2)∫r²dθ` D `2π∫r dθ`
87. `r=2` 一周所围面积？ A 2π B 4π C 8π D 16π
88. 极曲线在θ处水平切线通常要求？ A dx/dθ=0且dy/dθ≠0 B dy/dθ=0且dx/dθ≠0 C r=0 处的曲线一定存在水平切线 D dr/dθ=0 时该点切线水平
89. 极曲线 `r=2cosθ` 是？ A 圆 B 直线 C 心形 D 抛物线
90. 参数弧长公式？ A `∫(x'(t)²+y'(t)²)dt` B `∫sqrt(x'²+y'²)` C `∫x'(t)y'(t)dt` D `∫(x'(t)+y'(t))dt`

## 数列与级数

91. 若 `lim a_n=3`，则 `Σa_n`？ A 收敛到3 B 收敛到0 C 发散 D 条件收敛
92. `Σ_{n=0}∞(1/3)^n` 的和？ A 1/2 B 1 C 3/2 D 3
93. `Σ1/n²`？ A 发散 B 收敛 C 条件收敛 D 项不趋0
94. `Σ1/sqrt(n)`？ A 收敛 B 发散 C 几何 D 交错
95. `Σ(-1)^n/n`？ A 绝对收敛 B 条件收敛 C 发散因项不趋0 D 几何收敛
96. 比值判别极限L=0.4，级数？ A 绝对收敛 B 发散 C 条件收敛 D 无结论
97. 比值判别L=1，结论？ A 收敛 B 发散 C 绝对收敛 D 无结论
98. `Σ n!/3^n` 用比值判别的结果？ A 收敛 B 发散 C 几何 D 望远镜
99. 幂级数比值给 `|x-2|<5`，半径？ A 2 B 3 C 5 D 7
100. 上题收敛中心？ A -5 B 0 C 2 D 5
101. 求收敛区间时半径得到后还要？ A 对通项再求一次导数 B 检查两个端点 C 用比值判别法再算一次半径 D 把半径取倒数得到区间
102. Maclaurin `e^x` 前三项（含常数）？ A `1+x+x²/2` B `x+x²+x³` C `1-x+x²` D `1+x²/2`
103. `sinx` 的x³项系数？ A 1 B -1 C 1/6 D -1/6
104. `cosx` 的x²项？ A x² B -x²/2 C x²/2 D -x²
105. `1/(1-x)` 的级数是？ A `Σx^n,|x|<1` B `Σ(-x)^n` C `Σx^n/n!` D `Σnx^n`
106. 若 f 的 `(x-a)^4` 系数为7，则 `f^(4)(a)`=? A 7 B 28 C 168 D 4/7
107. 交错级数余项绝对值至多？ A 所有遗漏项之和 B 第一个遗漏项绝对值 C 最后一保留项 D 0
108. 绝对收敛意味着？ A 原级数必收敛 B 原级数必发散 C 仅交错级数 D 项不趋0
109. 条件收敛意味着？ A 原级数收敛则绝对值级数也收敛 B 原级数收敛、绝对值级数发散 C 原级数自身发散、绝对值级数收敛 D 该级数必定是收敛的几何级数
110. `Σ(x-1)^n/4^n` 的半径？ A 1/4 B 1 C 4 D ∞

## 综合

111. 若 `F(x)=∫_0^x(t²-1)dt`，F在何区间递增？ A `(-1,1)` 与 `(1,∞)` B `(-∞,-1)∪(1,∞)` C 在 `(-∞,∞)` 上递增 D 仅在 `(1,∞)` 上递增
112. 上题F在x=1？ A 局部最大 B 局部最小 C 拐点 D 不连续
113. `lim(x→0)(1-cosx)/x²`=? A 0 B 1/2 C 1 D ∞
114. `∫_0^π sinx dx`=? A 0 B 1 C 2 D π
115. `Σ_{n=1}∞1/[n(n+1)]` 的和？ A 0 B 1 C 2 D 发散
116. `dy/dx=2x,y(0)=3` 的解？ A x²+3 B 2x+3 C e^{2x}+2 D x²
117. 半径R的圆绕直径旋转，用圆盘法所得体积应为？ A πR² B `4πR³/3` C `2πR³` D πR³/3
118. 若 v(t)=t-2，在[0,4]总路程？ A 0 B 2 C 4 D 8
119. `f(x)=lnx` 在x=1的二次Taylor多项式？ A `(x-1)+(x-1)²/2` B `(x-1)-(x-1)²/2` C `1+(x-1)-(x-1)²/2` D `x-(x²/2)+(x³/3)`
120. 若正项级数a_n≤b_n且Σb_n收敛，则？ A Σa_n收敛 B Σa_n发散 C 无结论 D a_n不趋0
