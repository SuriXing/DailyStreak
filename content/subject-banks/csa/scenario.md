1. ArrayList&lt;Integer&gt; a = new ArrayList&lt;Integer&gt;();<br>int[] d = {2, 4, 5, 6, 8};<br>for (int v : d) { a.add(v); }<br>for (int i = 0; i &lt; a.size(); i++) { if (a.get(i) % 2 == 0) { a.remove(i); } }<br>System.out.println(a); 的输出，以及让"删除全部偶数"成立的循环写法是？ A. 输出 [4, 5, 8]；换成按元素删除的 remove 重载即可删净 B. 输出 [5]；认为改用增强 for 遍历并删除就能删净全部偶数 C. 输出 [4, 5, 8]；改为从末尾向前遍历删除才能删净所有偶数 D. 输出 [5]；认为原循环已经正确删掉全部偶数，不需要修改
2. static void modify(ArrayList&lt;Integer&gt; p) {<br>&nbsp;&nbsp;p.add(0, 99);<br>&nbsp;&nbsp;p = new ArrayList&lt;Integer&gt;();<br>&nbsp;&nbsp;p.add(7);<br>}<br>ArrayList&lt;Integer&gt; a = new ArrayList&lt;Integer&gt;();<br>a.add(1);<br>a.add(2);<br>modify(a);<br>System.out.println(a); A. 输出 [99, 1, 2]；p 与 a 指向同一列表，重新赋值只改本地引用 B. 输出 [99, 1, 2, 7]；p 始终是 a 的别名，两次 add 都写进同一个列表 C. 输出 [1, 2]；认为方法内对参数引用的修改不会传回调用者的列表 D. 输出 [7, 99, 1, 2]；p = new 相当于清空 a 之后再插入 7
3. int[][] t = {{5, 1}, {2, 7, 3}};<br>int s = 0;<br>for (int r = 0; r &lt; t.length; r++) {<br>&nbsp;&nbsp;for (int c = 0; c &lt;= t[r].length; c++) {<br>&nbsp;&nbsp;&nbsp;&nbsp;s += t[r][c];<br>&nbsp;&nbsp;}<br>}<br>System.out.println(s); 的运行结果是？ A. 输出 18；越界位置读到的默认值 0 不会影响总和 B. 输出 15；内层条件等价于按第一行的长度遍历两行 C. 编译错误；不规则的二维数组初始化在 Java 中不合法 D. 运行时抛出越界异常；r=0 时 c 取到非法下标 2
4. int[][] g = new int[2][2];<br>int[] row = g[0];<br>row[0] = 8;<br>g[1] = row;<br>row[1] = 9;<br>System.out.println(g[0][0] + " " + g[1][1]); A. 输出 0 0；数组之间的赋值都会复制出一份新数组 B. 输出 8 9；两行是同一个数组，row 的改动两处可见 C. 输出 0 9；row 是复制出来的行，不再关联 g[0] 这一行 D. 输出 8 0；g[1] = row 复制了当时的内容，之后互不影响
5. static void fill(int[] arr) {<br>&nbsp;&nbsp;for (int i = 0; i &lt; arr.length; i++) { arr[i] = arr[i] + 1; }<br>}<br>static void replace(int[] arr) { arr = new int[]{0, 0, 0}; }<br>int[] a = {1, 2, 3};<br>fill(a);<br>replace(a);<br>System.out.println(a[0] + "," + a[2]); A. 输出 2,4；fill 改了数组，replace 只改本地引用 B. 输出 0,0；认为 replace 里新建的数组也赋给了调用者的 a C. 输出 1,3；方法内对数组元素的修改不会影响调用者的数组 D. 编译错误；方法参数被隐式声明为 final，不允许重新赋值
6. int[] a = {3, 1, 4};<br>for (int x : a) { x = x * 2; }<br>学生本想让每个元素翻倍。关于数组最终内容和正确改法，哪项正确？ A. {6, 2, 8}；认为 x 是数组元素的别名，循环结束时已全部翻倍 B. {3, 1, 4}；应改成 a[x] = x * 2，因为把 x 当成了元素的下标 C. 编译错误；认为增强 for 的循环变量在循环体内不能被重新赋值 D. {3, 1, 4}；x 只是副本，须改用 a[i] = a[i] * 2
7. ArrayList&lt;String&gt; w = new ArrayList&lt;String&gt;();<br>w.add("a");<br>w.add("bb");<br>w.add("ccc");<br>ArrayList&lt;String&gt; r = new ArrayList&lt;String&gt;();<br>for (String s : w) { if (s.length() &gt; 1) { r.add(0, s); } }<br>System.out.println(r); A. [bb, ccc]；把 add(0, s) 当成在列表末尾追加元素 B. [ccc, bb]；只保留长度大于 1 的串，且每次插到索引 0 C. [ccc]；把 add(0, s) 当成替换索引 0 处的元素 D. 抛出越界异常；认为空列表上在索引 0 处插入是非法操作
8. class Animal { public String speak() { return "?"; } }<br>class Dog extends Animal { public String speak() { return "woof"; } public String fetch() { return "ball"; } }<br>Animal a = new Dog();<br>Dog d = (Dog) a;<br>System.out.println(a.speak() + d.fetch()); A. ?ball；认为变量 a 的声明类型就决定了最终调用哪个 speak 版本 B. 编译错误；认为父类引用必须先向下转型才能调用 speak 方法 C. woofball；按运行时对象绑定 speak，转型后可调 fetch D. 运行时抛类型转换异常；认为父类引用转成子类时一定会失败
9. class Shape { public double area() { return 0; } public String name() { return "shape"; } }<br>class Square extends Shape { public double area() { return 4; } }<br>class Circle extends Shape { public double area() { return 3; } public String name() { return "circle"; } }<br>Shape[] s = { new Square(), new Circle(), new Shape() };<br>double t = 0;<br>for (Shape p : s) { t += p.area(); }<br>System.out.println(t + " " + s[1].name()); A. 7.0 circle；area 按运行时对象调用，name 也被覆盖 B. 0.0 circle；Shape 变量只会调用父类定义的 area 实现 C. 7.0 shape；name 没有被覆盖，所以输出父类里定义的字符串 D. 运行时抛类型转换异常；因为遍历异构数组时都要向下转型
10. public class Ticket {<br>&nbsp;&nbsp;private static int sold = 0;<br>&nbsp;&nbsp;private int id;<br>&nbsp;&nbsp;public Ticket() { sold++; id = sold; }<br>&nbsp;&nbsp;public int getId() { return id; }<br>&nbsp;&nbsp;public static int getSold() { return sold; }<br>}<br>Ticket a = new Ticket();<br>Ticket b = new Ticket();<br>System.out.println(a.getId() + " " + b.getId() + " " + Ticket.getSold()); A. 0 1 2；先执行 id = sold 再执行 sold++，编号从 0 开始 B. 2 2 2；认为 id 也声明为 static，两个对象共用同一编号 C. 编译错误；认为构造方法体内不能访问类的静态字段 sold D. 1 2 2；sold 被所有对象共享，每次构造先自增再赋给 id
11. class P {<br>&nbsp;&nbsp;public P() { System.out.print("P"); }<br>&nbsp;&nbsp;public P(int x) { System.out.print("P" + x); }<br>}<br>class C extends P {<br>&nbsp;&nbsp;public C() { super(2); System.out.print("C"); }<br>}<br>new C(); A. PC；因为 super(2) 找不到匹配，退回调用父类无参构造器 B. P2C；先执行父类带参构造器输出 P2，再执行子类体输出 C C. CP2；认为子类构造器体先执行完，再执行父类的构造器 D. 编译错误；认为子类构造器只能通过 super() 调用无参构造器
12. abstract class Food {<br>&nbsp;&nbsp;public abstract double cost();<br>}<br>class Pizza extends Food {<br>&nbsp;&nbsp;private int slices;<br>&nbsp;&nbsp;public Pizza(int n) { slices = n; }<br>&nbsp;&nbsp;public double cost(int n) { return n * 3.0; }<br>}<br>Food f = new Pizza(4);<br>System.out.println(f.cost()); A. 运行时抛 AbstractMethodError；f.cost() 找不到实现 B. 编译错误；认为错在调用处，必须先把 f 转型为 Pizza 才行 C. 编译错误；cost(int) 是重载不是覆盖，应改成无参的 cost() D. 无错误；认为方法名相同就算实现了抽象方法，输出 12.0
13. public static int f(int n) {<br>&nbsp;&nbsp;if (n &lt;= 1) { return n; }<br>&nbsp;&nbsp;return f(n - 1) + f(n - 2);<br>}<br>调用 f(4) 后，返回值与 f 被调用的总次数（含初始调用）分别是？ A. 返回值 3，f 共被调用 9 次，含最初的 f(4) 调用 B. 返回值 5，f 共被调用 9 次，因为把 n &lt;= 1 都当成返回 1 C. 返回值 3，f 共被调用 8 次，因为没把初始调用计入次数 D. 返回值 3，f 共被调用 7 次，因为漏数了两次 f(0) 调用
14. public static String g(String s) {<br>&nbsp;&nbsp;if (s.length() &lt;= 1) { return s; }<br>&nbsp;&nbsp;return g(s.substring(1)) + s.charAt(0);<br>}<br>g("abc") 的返回值是？ A. bca；只完成最外层一次拼接，没有继续在子串上递归 B. abc；把拼接顺序记反，写成 charAt(0) + g(s) C. cba；每层先反转剩余子串，再把首字符接到结果末尾 D. 抛出越界异常；认为空串上调用 charAt(0) 也会越界失败
15. public static int h(int n) {<br>&nbsp;&nbsp;if (n == 0) { return 0; }<br>&nbsp;&nbsp;return n + h(n - 2);<br>}<br>h(5) 的结果是？ A. 返回 9；认为 n 递减到 1 时会自动满足终止条件 B. 抛出栈溢出错误；奇数 n 跳过 0，递归永远不会终止 C. 返回 15；忽略步长 2，把 h 当成计算 1 到 5 的连续整数和 D. 返回 8；认为最后剩下的 h(1) 不计入结果，只有 5 与 3 相加
16. public static int m(int[] a, int i) {<br>&nbsp;&nbsp;if (i == a.length) { return 0; }<br>&nbsp;&nbsp;int rest = m(a, i + 1);<br>&nbsp;&nbsp;return a[i] &gt; rest ? a[i] : rest;<br>}<br>int[] d = {3, 9, 4};<br>int r = m(d, 0);  // r 的值是？ A. 0；认为三元表达式最终返回的是基础情形的值 B. 3；认为没有比较过程，直接返回第一个元素 a[0] 的值 C. 4；认为递归返回的总是数组最后一个元素的值 D. 9；逐层把当前元素与右侧子问题的最大值比较后返回
17. public static void p(int n) {<br>&nbsp;&nbsp;if (n == 0) { return; }<br>&nbsp;&nbsp;p(n - 1);<br>&nbsp;&nbsp;System.out.print(n);<br>&nbsp;&nbsp;p(n - 1);<br>}<br>p(2); 的输出是？ A. 211；把打印语句放在两次递归调用之前，先打印再递归 B. 121；先走左侧递归到基础情形，再打印 n，再走右侧递归 C. 12；漏掉最后一行 p(n - 1) 调用，只走了左半边的递归 D. 112；把打印语句放在两次递归调用之后，两侧递归完才打印
18. int[] a = {5, 3, 8, 1, 9};<br>for (int i = 0; i &lt; 2; i++) {<br>&nbsp;&nbsp;int min = i;<br>&nbsp;&nbsp;for (int j = i + 1; j &lt; a.length; j++) { if (a[j] &lt; a[min]) { min = j; } }<br>&nbsp;&nbsp;int t = a[i]; a[i] = a[min]; a[min] = t;<br>}<br>循环结束后数组的内容，以及要完全升序还需的交换次数是？ A. {1, 3, 8, 5, 9}，还需 1 次交换；i=2 时把 5 换到索引 2 B. {1, 3, 8, 5, 9}，还需 2 次交换；认为剩余两趟都必然要交换 C. {1, 3, 5, 8, 9}，还需 0 次交换；认为两趟已排好前三个位置 D. {1, 1, 8, 1, 9}，还需 1 次交换；认为只用赋值把最小值搬到前面即可
19. int[] a = {2, 4, 6, 8, 10, 12, 14};<br>// 二分查找 12：lo=0，hi=a.length-1，mid=(lo+hi)/2，<br>// 每次把 a[mid] 与目标比较，命中即停止<br>依次得到的 mid 下标和总比较次数分别是？ A. 3、4、5，共 3 次；把 lo 写成 mid 而不是 mid + 1，多走一趟 B. 3、5，共 3 次；命中 12 后再与相邻元素比较一次作确认 C. 4、6，共 2 次；把下标按 1 开始编号，(1 + 7) / 2 得 4 D. 3、5，共 2 次；先比 a[3]=8 再比 a[5]=12，两次命中
20. int[] a = {4, 2, 7, 1};<br>// 插入排序：外层 i 从 1 到 a.length-1，把 a[i] 插入有序前缀，<br>// 每个被向后挪一位的元素记 1 次右移<br>执行完 i = 2 这一趟后的数组内容，以及整个排序的右移总次数是？ A. {1, 2, 4, 7}，共 4 次右移；认为 i=2 结束后数组已完全有序 B. {2, 4, 1, 7}，共 4 次右移；认为每趟都与右侧相邻元素交换位置 C. {2, 4, 7, 1}，共 4 次右移；i=2 时 7 已在位，只有 1 需要移动 D. {2, 4, 7, 1}，共 5 次右移；把比较次数当成右移次数（1+1+3=5），混淆比较与移动
21. public static int count(int[] a, int t) {<br>&nbsp;&nbsp;int c = 0;<br>&nbsp;&nbsp;for (int i = 0; i &lt; a.length; i++) {<br>&nbsp;&nbsp;&nbsp;&nbsp;if (a[i] == t) { c++; return c; }<br>&nbsp;&nbsp;}<br>&nbsp;&nbsp;return c;<br>}<br>int[] d = {3, 5, 3, 5};<br>count(d, 3) 的返回值与正确的修改是？ A. 返回 1；与预期 2 不符，应把 return c 移到循环外部 B. 返回 2，与预期一致；循环会继续比较后面的元素并累加计数 C. 编译错误；认为存在没有 return 的执行路径，所以方法缺少返回值 D. 返回 1；应把 c++ 改成 c = c + 1 让自增提前生效
22. int a = 7, b = 2;<br>double r = a / b;<br>double s = (double) a / b;<br>System.out.println(r + " " + s); A. 3.5 3.5；认为把结果赋给 double 会让右侧先按 double 运算 B. 3.0 3.5；第一行先做整数除法再拓宽，第二行强制转换后得小数 C. 3 3.5；认为 double 变量存整数值时打印结果不带小数点 D. 3.5 3.0；把 (double) 的作用位置记反，以为只有第一行被提升
23. char c = 'A';<br>int k = c + 1;<br>char d = (char) k;<br>System.out.println(k + " " + d); A. 66 A；认为 (char) 窄化转换不改变字符，d 仍是 'A' B. 66 66；认为 char 变量用 println 输出时会显示编码数字 C. 66 B；'A' 编码为 65，加 1 后转回 char 是 B D. 编译错误；认为 char 与 int 做算术运算时必须先显式转成 int
24. int i = 0;<br>boolean b = (i++ &gt; 0) &amp;&amp; (i++ &gt; 1);<br>System.out.println(i + " " + b); A. 2 false；认为 &amp;&amp; 两侧都会被求值，两次自增都会执行 B. 2 true；把后缀 i++ 当成前缀 ++i，于是两个条件都成立 C. 0 false；认为整个布尔表达式失败时其中的自增都不生效 D. 1 false；左侧为 false 短路，第二次自增不再执行
25. int i = 0, sum = 0;<br>while (i &lt; 5) {<br>&nbsp;&nbsp;i++;<br>&nbsp;&nbsp;if (i % 2 == 0) { continue; }<br>&nbsp;&nbsp;sum += i;<br>}<br>System.out.println(sum + " " + i); A. 9 5；只累加奇数 1、3、5，i 到 5 后退出循环 B. 15 5；忽略 if 与 continue，把 1 到 5 全部累加 C. 1 2；把 continue 当成 break，遇到偶数就退出循环 D. 6 5；把取余条件看反，只把偶数 2 与 4 累加进去
