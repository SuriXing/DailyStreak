|题号|答案|解析|
|---|---|---|
|1|C|一句话解析：A 换成按元素删除的重载后下标仍会前进，被左移的 4 被跳过；B 在增强 for 中删除会抛 ConcurrentModificationException；D 实际输出会留下偶数 8，说明并未删净。|
|2|A|一句话解析：B 误以为 p 重新赋值后仍指向 a；C 忽略了 p.add(0, 99) 已经改动了 a；D 把 p = new 误解成清空原列表再插入。|
|3|D|一句话解析：A 越界会直接抛异常，读不到默认值 0；B 内层条件是 c &lt;= t[r].length，并不会按第一行的长度截断；C 锯齿数组的这种初始化在 Java 中完全合法。|
|4|B|一句话解析：A 数组赋值不复制元素，两个变量会指向同一数组；C 忽略了 int[] row = g[0] 赋的是引用；D 忽略了 g[1] = row 之后两行共享同一数组。|
|5|A|一句话解析：B replace 只改了形参引用，没有写回 a；C fill 是通过引用修改元素的，会影响调用者的数组；D Java 方法参数不是 final，重新赋值完全合法。|
|6|D|一句话解析：A 增强 for 的 x 与数组元素没有引用关系；B x 是元素值而不是下标，a[x] 会越界；C 这段代码能正常编译，只是不会改到数组。|
|7|B|一句话解析：A add(0, s) 是插入而不是末尾追加；C add 不是 set，不会替换已有元素；D 在空列表索引 0 处插入是合法的，不会抛异常。|
|8|C|一句话解析：A 被覆盖的方法按运行时类型选版本，与声明类型无关；B 父类引用可以直接调用 speak，不需要先转型；D 实际对象就是 Dog，向下转型不会失败。|
|9|A|一句话解析：B 动态绑定会调用子类覆盖后的 area；C name 已在 Circle 中被覆盖；D 父类引用调用被覆盖的方法并不需要向下转型。|
|10|D|一句话解析：A 代码先自增再赋值，编号从 1 开始；B id 是实例字段，每个对象各有一份；C 在构造方法里访问静态字段完全合法。|
|11|B|一句话解析：A 显式 super(2) 能匹配到父类带参构造器，不会退回无参版本；C 父类构造器一定先于子类构造器体执行；D 子类构造器可以调用父类的带参构造器。|
|12|C|一句话解析：A 抽象方法未实现是编译期错误，不会拖到运行时；B 错误出在 Pizza 的类声明上，转型也解决不了；D 方法名相同不等于覆盖，参数列表不同只是重载。|
|13|A|一句话解析：B 基础情形的返回值并非都是 1，f(0) 返回 0；C 初始调用 f(4) 也要计入总次数；D f(0) 确实被调用了两次，不能漏数。|
|14|C|一句话解析：A 代码会继续在子串上递归，不会只做最外层一次；B 拼接顺序是递归结果在前，不是 charAt(0) 在前；D 基础情形拦住了空串，charAt(0) 不会被调用。|
|15|B|一句话解析：A n == 0 是唯一终止条件，奇数序列取不到 0；C 步长是 2，并不是逐个累加 1 到 5；D 递归根本不会结束，不存在剩下 h(1) 的结果。|
|16|D|一句话解析：A 基础情形只作为比较初值，最终返回的是最大值；B 每层都做了比较，不是直接返回 a[0]；C 返回值由比较结果决定，与最后一个元素无关。|
|17|B|一句话解析：A 打印写在递归之前会先输出 2；C 漏掉最后一次 p(n - 1) 会少输出一个 1；D 打印写在两次递归之后会得到 112。|
|18|A|一句话解析：B 剩余两趟里只有 i=2 需要交换；C 两趟只固定了前两个位置，数组并未有序；D 代码用的是三变量交换，不会用赋值覆盖掉元素 5。|
|19|D|一句话解析：A 把 lo 写成 mid 会多比较一趟，mid 会多出 4；B 命中后立即停止，不需要再比较相邻元素；C 数组下标从 0 开始，mid 不可能是 4、6。|
|20|C|一句话解析：A 执行完 i=2 时 1 还没被插入，数组并未有序；B 插入排序是右移腾位，不是交换相邻元素；D 右移次数是 1+0+3=4，5 是比较次数。|
|21|A|一句话解析：B return 写在循环体内，命中第一次就结束方法；C 方法末尾已有 return，能通过编译；D c++ 自增是生效的，真正的问题是提前返回。|
|22|B|一句话解析：A 赋值目标的类型不会影响右侧的整数除法；C double 变量打印整数值时仍带小数点；D 提升只发生在第二行，第一行依旧是整数除法。|
|23|C|一句话解析：A (char) 确实把 66 转成了字符 B；B println 打印 char 变量输出的是字符本身；D char 与 int 混合运算会自动提升，不需要显式转换。|
|24|D|一句话解析：A &amp;&amp; 左侧为 false 时右侧根本不求值；B i++ 先用旧值 0 比较，左侧是 false；C 左侧的 i++ 已经执行，i 不会回到 0。|
|25|A|一句话解析：B continue 会跳过偶数，不是把 1 到 5 全加；C continue 只跳过本轮，不会结束循环；D 条件是偶数时跳过，累加的是奇数。|
